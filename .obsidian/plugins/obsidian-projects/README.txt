After installing the Projects community plugin, create a project:

1. Data source: Folder → path `components`
2. Add views (Board, Table, Calendar, etc.) and fields in the plugin UI

Plugin files (data.json) are stored under this folder once configured.
